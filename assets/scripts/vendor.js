const calculateButton = document.getElementById('calculate-button');
const resetButton = document.getElementById('reset-button');
const weightInput = document.getElementById('weight-input');
const heightInput = document.getElementById('height-input');

const showErrorWeight = document.getElementById('alert-weight');
const showErrorHeight = document.getElementById('alert-height');

const showCalculateBMI = document.getElementById('show-bmi');
const showErrorCalculateBMI = document.getElementById('alert-error');